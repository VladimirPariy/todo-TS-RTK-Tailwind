import {createSlice, PayloadAction} from "@reduxjs/toolkit";import {RootState} from "../../hooks/useAppSelector";const validTodoSlice = createSlice({    name: '@@isValid',    initialState: true,    reducers: {        isValidTodo: (state, action: PayloadAction<boolean>) => {            return action.payload        }    }})export const {isValidTodo} = validTodoSlice.actionsexport const isValidReducer = validTodoSlice.reducerexport const isValidTodoSelector = (state: RootState) => state.isValid