import {useAppSelector} from "./useAppSelector";import {themeSelector} from "../store/slice/themeSlice";import cl from "../App.module.scss";type stylesModuleType = ReturnType<typeof cl>type themeType = (nameOfClass:string, styles:stylesModuleType) =>stringexport const useTheme:themeType = (nameOfClass, styles) => {    const theme = useAppSelector(themeSelector)    return [styles[nameOfClass], styles[theme === 'light' ? 'light' : 'dark']].join(' ')}